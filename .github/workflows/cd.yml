# name: Continuous Deployment

# on:
#   push:
#     branches:
#       - main
#       - dev

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v2

#       - name: Set up QEMU
#         uses: docker/setup-qemu-action@v1

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v1

#       - name: Log in to Docker Hub
#         uses: docker/login-action@v1
#         with:
#           username: ${{ secrets.DOCKER_USERNAME }}
#           password: ${{ secrets.DOCKER_PASSWORD }}

#       - name: Build and push Docker images
#         uses: docker/build-push-action@v2
#         with:
#           context: .
#           push: true
#           tags: your-dockerhub-username/devopsify-ecommerce:latest

#       - name: Set up kubectl
#         uses: azure/setup-kubectl@v1
#         with:
#           version: 'latest'

#       - name: Deploy to Kubernetes
#         run: |
#           kubectl apply -f infrastructure/kubernetes/manifests/your-app-deployment.yaml
#           kubectl apply -f infrastructure/kubernetes/manifests/your-app-service.yaml

#       - name: Notify deployment success
#         run: echo "Deployment to Kubernetes was successful!"